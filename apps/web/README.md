# CaptureLock Operator Web UI (Placeholder)

This package is intentionally reserved for the future operator/demo UI (Next.js / React / TypeScript).

Per the Phase 0 bootstrap specification:

- The core backend and capture-time verification engine are the primary focus.
- Frontend implementation will begin in a future phase once the core verification contracts and Razorpay test-mode integration are stabilized.
